@model HelloDoc.DAL.ViewModels.AdminDashvm



<div class="modal fade" id="clearModalId" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered ">
        <div class="modal-content pt-4 pb-4 border rounded-3">
            <form id="clearCaseForm" onsubmit="ClearCase(@Model.clearCaseModel.RequestId)">
                <input type="text" asp-for="@Model.clearCaseModel.RequestId" hidden/>
                <input type="text" asp-for="@Model.StatusForName" id="statusForName"  hidden/>


                <div class="container d-flex flex-column pt-4 pb-4">
                    <div class="container d-flex justify-content-center">
                        <i class="bi bi-exclamation-circle-fill text-warning modal-icon display-1"></i>
                    </div>
                    <div class="container d-flex justify-content-center fs-3 fw-semibold mt-1">
                        Confirmation for clear case
                    </div>
                </div>
                <div class="container  pb-5 text-center">
                    Are sure you want to clear this request? Once clear this <br />
                    request then you are not able to see this request
                </div>
                <div class="d-flex justify-content-center pb-4">
                    <button type="submit" class="btn btn-info text-white me-2" >Clear</button>
                    <button type="button" class="btn  text-info border-info" data-bs-dismiss="modal">Cancel</button>
                </div>
            </form>
        </div>
    </div>
</div>











@* block model *@

@* <div class="modal fade" id="clearModalId" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-info text-white">
                <h1 class="modal-title fs-5" id="exampleModalLabel">Confirm Block</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <form id="blockCaseForm" method="post" onsubmit="BlockCase(@Model.blockCaseModel.RequestId)">
                <div class="modal-body">
                    <input type="text" asp-for="@Model.blockCaseModel.RequestId" />
                    <div class="text-secondary">
                        Patient Name : <span class="text-info"> @Model.blockCaseModel.Name</span>
                    </div>
                    <div class="mt-4">
                        <div class="form-floating">
                            <textarea class="form-control mb-3 " asp-for="@Model.blockCaseModel.Reason" style="height:100px" placeholder="Leave a comment here" id="floatingTextarea3"></textarea>
                            <label for="floatingTextarea3" class="text-secondary">Reason For Block Request</label>
                        </div>
                    </div>
                    <div class="d-flex justify-content-end">
                        <button type="submit" class="btn btn-info text-white me-2" data-bs-dismiss="modal">Confirm</button>
                        <button type="button" class="btn  text-info border-info" data-bs-dismiss="modal">Cancel</button>
                    </div>
                </div>
            </form>


        </div>
    </div>
</div> *@